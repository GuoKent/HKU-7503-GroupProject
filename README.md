# HKU-7503-GroupProject

## Run the file 
`quantize_model.py`: Model quantization code, run it 

`vllm.py`: vllm accelerated inference framework call, Linux environment is required 

`kv_cache.py`: Compare the effect of kv cache, run it

`sft.py`: Code for fine-tuning

`evaluate.py`: Evaluate the effectiveness of model generation

`res_visual.ipynb`: Visualization of results